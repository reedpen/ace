# Data Ingestion Log

This file documents all locations in the `src2` codebase where external data is read into the system. This log is intended to help developers identify where changes may be needed if data formats change.

## Miniscope Module

### Video Data (`.avi`, `.tif`)
*   **File:** `src2/miniscope/miniscope_data_manager.py`
    *   **Line 114:** `movie = cm.load(filename)`
    *   **Line 240:** `movie = cm.load_movie_chain(filenames)`
    *   **Line 242:** `movie = cm.load(filenames)`
    *   **Context:** Used to load raw calcium imaging videos using CaImAn.

*   **File:** `src2/miniscope/movie_io.py`
    *   **Line 36:** `return cm.load(path)`
    *   **Context:** Helper function for loading movies.

*   **File:** `src2/miniscope/miniscope_processor.py`
    *   **Line 180:** `mc_movie = cm.load(mc.mmap_file)`
    *   **Line 272:** `mean_img = np.mean(cm.load(mc.fname[0]), 0)[12:-12, 12:-12]`
    *   **Context:** Loading processed or intermediate movie files (often memory-mapped).

### Metadata (`.json`)
*   **File:** `src2/miniscope/miniscope_data_manager.py`
    *   **Line 134:** `data = json.loads(f.read())`
    *   **Line 147:** `data2 = json.loads(d.read())`
    *   **Line 184:** `metadata = json.load(file)`
    *   **Context:** Reading `metaData.json` files generated by the Miniscope software.

### Timestamps & Events (`.csv`)
*   **File:** `src2/miniscope/miniscope_data_manager.py`
    *   **Line 210:** `with open(file_path, newline='') as t:`
    *   **Line 272:** `with open(miniscope_events_filepath, newline='') as t:`
    *   **Context:** Reading `timeStamps.csv` and `notes.csv` (events) using the standard `csv` library.

### Memory Mapped Files (`.mmap`)
*   **File:** `src2/miniscope/miniscope_processor.py`
    *   **Line 123:** `Yr, dims, T = cm.load_memmap(dm.opts_caiman.get('data', 'fnames')[0])`
    *   **Context:** Loading memory-mapped files for efficient CaImAn processing.

*   **File:** `src2/miniscope/miniscope_postprocessor.py`
    *   **Line 104:** `Yr, dims, T = cm.load_memmap(opts_caiman.get('data', 'fnames')[0])`
    *   **Line 191:** `Yr, dims, T = cm.load_memmap(dm.opts_caiman.get('data', 'fnames')[0])`
    *   **Context:** Accessing raw data during post-processing visualization/refinement.

## Ephys Module

### Neuralynx Data (`.nev`, `.ncs`)
*   **File:** `src2/ephys/ephys_data_manager.py`
    *   **Line 50:** `file_reader = NeuralynxIO(dirname=ephys_dir_path)`
    *   **Context:** Uses `neo.io.NeuralynxIO` to ingest the entire directory of Neuralynx files.

## Shared/Utility

### Configuration & CSVs
*   **File:** `src2/shared/csv_worker.py`
    *   **Line 10:** `df = pd.read_csv(csv_file)`
    *   **Context:** Generic CSV reading for parameter files or dataframes.

*   **File:** `src2/shared/file_downloader.py`
    *   **Line 48:** `df = pd.read_csv(csv_path, index_col="line number")`
    *   **Context:** Reading the `experiments.csv` file to locate data.

### Binary/Raw File IO
*   **File:** `src2/shared/file_downloader.py`
    *   **Line 136:** `with open(f"{BASE_FILE_PATH}/{path}/Miniscope/{sub_item.name}", "wb") as output_file:`
    *   **Context:** Writing downloaded binary data from Box to disk.

### NumPy Archives (`.npz`, `.npy`)
*   **File:** `src2/multimodal/poster_draft.py`
    *   **Line 116:** `miniscope_data_manager.mean_fluorescence_dict = np.load(calcium_signal_filepath)`
    *   **Context:** Loading pre-computed mean fluorescence data.

*   **File:** `src2/miniscope/miniscope_processor.py`
    *   **Line 267:** `with np.load(fl) as ld:`
    *   **Context:** Loading metrics files generated during motion correction.
