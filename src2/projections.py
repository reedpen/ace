

class Projections():
    def __init__(self, max: float, std: float, min: float, mean: float, median: float, range: float, time: float):
        self.max = max
        self.std = std
        self.min = min
        self.mean = mean
        self.median = median
        self.range = range
        self.time = time